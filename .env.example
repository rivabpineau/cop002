# AWS Configuration
# Default region for AWS Bedrock (optional - defaults to us-east-1)
BEDROCK_REGION=us-east-1

# Optional AWS Profile (uses default if not specified)
# AWS_PROFILE=default

# Backend Configuration (optional - used by UI)
# BACKEND_URL=http://localhost:8000